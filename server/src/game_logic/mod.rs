/// game_logic/mod.rs — Scalable, data-driven game engine.
/// 
/// Modules:
///   roles   — RoleType (build-time generated), SkillType, Skill, Role
///   engine  — GameContext, execute_skill, resolve_night, resolve_vote, check_win

pub mod roles;
pub mod engine;
pub mod room_task;
